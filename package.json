{
  "name": "soulkey",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "cd nextjs && npm run dev",
    "build": "cd nextjs && npm run build",
    "build:contracts": "cd foundry && forge build",
    "test:contracts": "cd foundry && forge test",
    "install:all": "cd nextjs && npm install"
  }
}
