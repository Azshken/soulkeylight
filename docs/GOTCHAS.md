# SoulKey — Gotchas

Stub (12/09/26): the full gotcha log referenced by CLAUDE.md was never committed. The hard-won
lessons currently live where they bite — the ⚠️ notes in CLAUDE.md (65-byte HKDF IKM, AES-copy
retention, SESSION_SECRET, address checksumming, per-contract token IDs), the "Critical Gotchas"
section of docs/skills/SKILL_TESTING.md, and the fix commits in git history. Add a short entry
here when a new gotcha surfaces, each with the commit or issue that proved it; do not duplicate
the skill docs.
